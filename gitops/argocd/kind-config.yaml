apiVersion: kind.x-k8s.io/v1alpha4
kind: Cluster
name: expensy-cluster
nodes:
  - role: control-plane # Панель управления (ArgoCD, API-server)
  
  - role: worker # Сюда пойдут MongoDB, Prometheus, Loki и Jaeger
    name: expensy-cluster-worker
    extraMounts:
      - hostPath: /Users/musaxasmammedov/expensy-data
        containerPath: /mnt/expensy-data
