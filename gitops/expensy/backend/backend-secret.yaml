# yaml-language-server: $schema=https://www.schemastore.org/any.json
#
# ExternalSecret для сборки секретов backend из Azure Key Vault.
# Создает Kubernetes Secret 'expensy-backend-secrets' в namespace expensy
# с ключами DATABASE_URI и REDIS_PASSWORD.
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: expensy-backend-secrets
  namespace: expensy
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: azure-keyvault-backend
    kind: ClusterSecretStore
  target:
    name: expensy-backend-secrets
    creationPolicy: Owner
  data:
    - secretKey: DATABASE_URI
      remoteRef:
        key: backend-database-uri
    - secretKey: REDIS_PASSWORD
      remoteRef:
        key: redis-password
